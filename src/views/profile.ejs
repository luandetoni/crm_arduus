<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <!-- Google Fonts - Poppins -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Main CSS -->
    <link rel="stylesheet" href="/css/style.css">
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/favicon.png">
</head>
<body>
    <div class="dashboard">
        <%- include('partials/sidebar') %>
        
        <div class="main-content">
            <%- include('partials/header') %>
            
            <div class="content">
                <div class="d-flex align-center justify-between mb-20">
                    <h1 class="page-title">Meu Perfil</h1>
                </div>
                
                <div class="row">
                    <div class="col" style="flex: 0 0 250px;">
                        <div class="card">
                            <div class="card-body text-center">
                                <div class="mb-20">
                                    <img src="https://via.placeholder.com/150" alt="Foto de Perfil" class="profile-avatar" style="width: 150px; height: 150px; border-radius: 50%;">
                                </div>
                                <h3 id="profileName">Nome do Usuário</h3>
                                <p class="text-secondary" id="profileRole">Cargo: Administrador</p>
                                
                                <div class="mt-20">
                                    <button class="btn btn-primary btn-block mb-10">
                                        <i class="fas fa-camera"></i> Alterar Foto
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col" style="flex: 1;">
                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title">Informações Pessoais</h3>
                            </div>
                            <div class="card-body">
                                <form id="profileForm">
                                    <div class="form-row">
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="name" class="form-label">Nome Completo</label>
                                                <input type="text" id="name" name="name" class="form-control" value="Nome do Usuário">
                                            </div>
                                        </div>
                                        
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="email" class="form-label">Email</label>
                                                <input type="email" id="email" name="email" class="form-control" value="usuario@example.com" readonly>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <div class="form-row">
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="phone" class="form-label">Telefone</label>
                                                <input type="tel" id="phone" name="phone" class="form-control" value="(11) 98765-4321">
                                            </div>
                                        </div>
                                        
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="department" class="form-label">Departamento</label>
                                                <select id="department" name="department" class="form-control">
                                                    <option value="vendas">Vendas</option>
                                                    <option value="marketing">Marketing</option>
                                                    <option value="suporte">Suporte</option>
                                                    <option value="administrativo" selected>Administrativo</option>
                                                    <option value="tecnologia">Tecnologia</option>
                                                    <option value="outros">Outros</option>
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <div class="form-row">
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="jobTitle" class="form-label">Cargo</label>
                                                <input type="text" id="jobTitle" name="jobTitle" class="form-control" value="Administrador do Sistema">
                                            </div>
                                        </div>
                                        
                                        <div class="form-col">
                                            <!-- Coluna vazia para manter o layout -->
                                        </div>
                                    </div>
                                    
                                    <div class="form-actions">
                                        <button type="submit" class="btn btn-primary">Salvar Alterações</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                        
                        <div class="card mt-20">
                            <div class="card-header">
                                <h3 class="card-title">Alterar Senha</h3>
                            </div>
                            <div class="card-body">
                                <form id="passwordForm">
                                    <div class="form-row">
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="currentPassword" class="form-label">Senha Atual</label>
                                                <input type="password" id="currentPassword" name="currentPassword" class="form-control" required>
                                            </div>
                                        </div>
                                        
                                        <div class="form-col">
                                            <!-- Coluna vazia para manter o layout -->
                                        </div>
                                    </div>
                                    
                                    <div class="form-row">
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="newPassword" class="form-label">Nova Senha</label>
                                                <input type="password" id="newPassword" name="newPassword" class="form-control" required>
                                            </div>
                                        </div>
                                        
                                        <div class="form-col">
                                            <div class="form-group">
                                                <label for="confirmPassword" class="form-label">Confirmar Nova Senha</label>
                                                <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" required>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <div class="form-actions">
                                        <button type="submit" class="btn btn-primary">Alterar Senha</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- jQuery (necessário para alguns plugins Bootstrap) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Main JavaScript -->
    <script src="/js/main.js"></script>
    <script>
        $(document).ready(function() {
            // Carregar dados do perfil
            const storedUser = JSON.parse(localStorage.getItem('currentUser') || '{}');
            
            if (storedUser.name) {
                $('#profileName').text(storedUser.name);
                $('#name').val(storedUser.name);
            }
            
            if (storedUser.email) {
                $('#email').val(storedUser.email);
            }
            
            if (storedUser.role) {
                $('#profileRole').text(`Cargo: ${storedUser.role}`);
            }
            
            // Form submits
            $('#profileForm').submit(function(e) {
                e.preventDefault();
                
                const formData = {
                    name: $('#name').val(),
                    phone: $('#phone').val(),
                    department: $('#department').val(),
                    jobTitle: $('#jobTitle').val()
                };
                
                console.log('Atualizando perfil:', formData);
                
                // Em uma aplicação real:
                // fetchAPI('auth/profile', {
                //    method: 'PUT',
                //    body: JSON.stringify(formData)
                // })
                // .then(response => {
                //    // Atualizar dados do usuário
                //    const updatedUser = { ...storedUser, ...formData };
                //    localStorage.setItem('currentUser', JSON.stringify(updatedUser));
                //    
                //    // Atualizar UI
                //    $('#profileName').text(updatedUser.name);
                //    
                //    showNotification('Perfil atualizado com sucesso', 'success');
                // })
                // .catch(error => {
                //    console.error('Erro ao atualizar perfil:', error);
                //    showNotification('Erro ao atualizar perfil', 'error');
                // });
                
                // Simulação
                showNotification('Perfil atualizado com sucesso', 'success');
            });
            
            $('#passwordForm').submit(function(e) {
                e.preventDefault();
                
                const currentPassword = $('#currentPassword').val();
                const newPassword = $('#newPassword').val();
                const confirmPassword = $('#confirmPassword').val();
                
                if (newPassword !== confirmPassword) {
                    alert('As senhas não coincidem');
                    return;
                }
                
                console.log('Atualizando senha...');
                
                // Em uma aplicação real:
                // fetchAPI('auth/change-password', {
                //    method: 'POST',
                //    body: JSON.stringify({
                //        currentPassword,
                //        newPassword
                //    })
                // })
                // .then(() => {
                //    $('#passwordForm')[0].reset();
                //    showNotification('Senha alterada com sucesso', 'success');
                // })
                // .catch(error => {
                //    console.error('Erro ao alterar senha:', error);
                //    showNotification('Erro ao alterar senha', 'error');
                // });
                
                // Simulação
                $('#passwordForm')[0].reset();
                showNotification('Senha alterada com sucesso', 'success');
            });
            
            // Função para mostrar notificações
            function showNotification(message, type) {
                alert(message);
            }
        });
    </script>
</body>
</html>